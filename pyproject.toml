[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tiny-transformer"
version = "0.1.0"
description = "Pure-NumPy autograd engine and GPT language model"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24"]

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
tiny-train = "training_cli:main"
tiny-train-safe = "training_cli:safe_main"
tiny-stream = "streaming_cli:main"
tiny-stream-benchmark = "streaming_benchmark:main"
tiny-gqa-convert = "gqa_convert_cli:main"
tiny-gqa-benchmark = "gqa_benchmark:main"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = [
    "train",
    "safe_train_cli",
    "training_cli",
    "tokenizer",
    "benchmark",
    "streaming_cli",
    "streaming_benchmark",
    "gqa_convert_cli",
    "gqa_benchmark",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
