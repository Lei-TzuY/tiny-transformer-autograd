name: tests

on:
  push:
  pull_request:

jobs:
  pytest:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install ".[dev]"
      - name: Smoke-test installed package and CLI
        run: |
          python -c "import engine, nn, train, tokenizer, benchmark, streaming_cli"
          tiny-train --help
          tiny-stream --help
      - name: Smoke-test benchmark report
        run: |
          python -m benchmark --vocab 16 --ctx 4 --d 8 --heads 2 --layers 1 --batch 1 --steps 1 --generate 1 --warmup 0 --repeats 2 --json
      - name: Run tests
        run: python -m pytest tests -q -W error
      - name: Compile sources
        run: python -m compileall -q src tests
